GEMINI_API_KEY=

# Client-side (VITE_ prefix required by Vite to expose it in the browser
# bundle) — Mixpanel project token. Missing/placeholder = analytics is a
# clean no-op, see src/lib/analytics.js.
VITE_MIXPANEL_TOKEN=
